# Mouse Domination CRM - Environment Configuration
# Copy this file to .env and configure for your environment

# =============================================================================
# FLASK CONFIGURATION
# =============================================================================
FLASK_ENV=production
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=generate-with-python-c-import-secrets-print-secrets-token_hex-32

# =============================================================================
# DATABASE
# =============================================================================
# Local development with PostgreSQL (recommended for dev/prod parity):
#   1. Start local postgres: docker compose -f deploy/docker-compose.dev.yml up -d
#   2. Uncomment the DATABASE_URL below
# DATABASE_URL=postgresql://mousedom:mousedom@localhost:5433/mousedom

# If DATABASE_URL is not set, defaults to SQLite (mouse_domination.db)

# Production Docker Compose uses these for PostgreSQL:
POSTGRES_USER=mousedom
POSTGRES_PASSWORD=change-this-to-a-strong-password
POSTGRES_DB=mousedom

# =============================================================================
# GUNICORN (Production WSGI Server)
# =============================================================================
GUNICORN_WORKERS=2
GUNICORN_BIND=0.0.0.0:5000
GUNICORN_LOG_LEVEL=info

# =============================================================================
# CADDY / DOMAIN (for HTTPS)
# =============================================================================
# Set to your domain for automatic HTTPS, or localhost for local testing
DOMAIN=localhost

# =============================================================================
# GOOGLE OAUTH (Optional - for "Sign in with Google")
# =============================================================================
# Get credentials from Google Cloud Console:
# https://console.cloud.google.com/apis/credentials
# Add redirect URI: http://127.0.0.1:5001/auth/google/callback (local)
# Add redirect URI: https://your-domain.com/auth/google/callback (production)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# =============================================================================
# YOUTUBE API (Optional - for channel stats)
# =============================================================================
YOUTUBE_API_KEY=
YOUTUBE_CHANNEL_ID=

# =============================================================================
# CREATOR INFO (for outreach templates)
# =============================================================================
CREATOR_CHANNEL_NAME=dazztrazak
CREATOR_CHANNEL_STATS=4,000+ subscribers

# =============================================================================
# PUBLIC API (for dazztrazak.com frontend)
# =============================================================================
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
PUBLIC_API_KEY=
